version: '3.8'

services:
  resume-q:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resume-q
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    ports:
      - "80:3000"
    restart: unless-stopped
